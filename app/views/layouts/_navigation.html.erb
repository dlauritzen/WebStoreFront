<%= link_to "Home", root_path, :class => 'brand' %>

<% if user_signed_in? %>
<ul class="nav">
</ul>
<ul class="nav pull-right">
  <li><%= link_to "#{current_user['name']} - Sign Out", destroy_user_session_path, :method => 'delete'%></li>
</ul>
<% end %>
